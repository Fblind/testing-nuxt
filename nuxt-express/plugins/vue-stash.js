import Vue from 'vue'
import VueStash from 'vue-stash'

Vue.use(VueStash)

/*
// only use plugin on the browser
if (process.BROWSER_BUILD) {
  Vue.use(VueStash)
}

// only use plugin on the server
if (process.SERVER_BUILD) {
  Vue.use(VueStash)
}
*/